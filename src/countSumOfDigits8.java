import java.util.Scanner;

public class countSumOfDigits8 {

	public static void main(String[] args) {
		// We need to count the sum of digits 
		// We need to extract each digit one by one 
		// add all digits to sum variable
		
		// get the number using Scanner class
		Scanner sc = new Scanner(System.in);
		int num = sc.nextint();
		}
}
